#!/bin/sh
# Usage: addr [<if>]
# Show IPv4 address for interface <if> or all interfaces with
# no <if> given.
#
# (C) 2010 Ryan Tomayko <tomayko.com>

ifconfig -a        |
grep 'inet '       |
grep broadcast     |
awk '{ print $2 }'

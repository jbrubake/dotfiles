# TODO: These are also set in ~/.environment. Can they be set in a single location?
# Make python use ~/lib instead of ~/.local/lib
PYTHONUSERBASE=$HOME
XDG_DATA_HOME=$HOME/share
XDG_CACHE_HOME=$HOME/var/cache
XDG_STATE_HOME=$HOME/var/lib


# vi: filetype=muttrc
#
# Mutt color settings
#
# Available colors
# white
# black
# green
# magenta
# blue
# cyan
# yellow
# red
# default
# bright<color>

## Miscellaneous color definitions ##
color normal     brightwhite    default
color bold       blue           default
color underline  green          default

color indicator  black          yellow        # Current selection
color status     brightyellow   blue
color search     black          brightwhite
color error      brightred      default
color message    magenta        default
color tilde      green          default
color tree       red            default

color quoted     cyan           default
color quoted1    magenta        default
color quoted2    yellow         default
color attachment white          default
color signature  brightgreen    default
color markers    red            default     # Marker at beginning of wrapped lines

## Colors for the headers in the reader ##
color hdrdefault white          default   # Default header colors
color header     green          default "^date: "
color header     cyan           default "^from: "
color header     yellow         default "^to: "
color header     brightblue     default "^cc: "
color header     brightblue     default "^bcc: "
color header     magenta        default "^subject: "
color header     red            default "^priority: "
color header     white          default "^x-mailer: "
color header     white          default "^user-agent: "

## Colors for the index ##
color index      brightblue     default ~N      # New
color index      red            default ~F      # Flagged
color index      white          default ~T      # Tagged
color index      brightwhite    default ~D      # Deleted

## Highlights inside the body of a message ##

# URLs
color body       brightblue     default "(http|https|ftp|news|telnet|finger)://[^ \">\t\r\n]*"
color body       brightblue     default "mailto:[-a-z_0-9.]+@[-a-z_0-9.]+"
color body       brightblue     default "news:[^ \">\t\r\n]*"

# email addresses
color body       brightblue     default "[-a-z_0-9.%$]+@[-a-z_0-9.]+\\.[-a-z][-a-z]+"

# Various smilies and the like
color body       brightgreen    default "<[Gg]>"                            # <g>
color body       brightgreen    default "<[Bb][Gg]>"                        # <bg>
color body       brightgreen    default " [;:]-*[)>(<|]"                    # :-) etc...
color body       brightblue     default "(^|[[:space:]])\\*[^[:space:]]+\\*([[:space:]]|$)" # *Bold* text.
color body       brightblue     default "(^|[[:space:]])_[^[:space:]]+_([[:space:]]|$)" # _Underlined_ text.
color body       brightblue     default "(^|[[:space:]])/[^[:space:]]+/([[:space:]]|$)" # /Italic/ text.
